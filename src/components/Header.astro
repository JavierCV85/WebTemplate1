---

---

<nav id="navHeader" class="fixed w-full z-20 top-0 start-0 h-20 bg-transparent transition-all duration-150">
    <div class="max-w-screen-xl flex flex-wrap items-center justify-between mx-auto p-4">
        <a href="/" class="flex items-center space-x-3 rtl:space-x-reverse">
            <img src="/logo.webp" class="h-14" alt="Flowbite Logo">
        </a>
        <div class="flex md:order-2 space-x-3 md:space-x-0 rtl:space-x-reverse">
            <!-- Aquí ha de ir el desplegable de redes sociales -->
        </div>
    <div class="items-center justify-between hidden w-full md:flex md:w-auto md:order-1" id="navbar-sticky">
      <ul class="flex flex-col p-4 md:p-0 mt-4 font-medium md:space-x-8 rtl:space-x-reverse md:flex-row md:mt-0 ">
        <li>
            <a href="#" class="py-2 px-3 font-overpass uppercase text-gray-900 rounded hover:bg-gray-100 md:hover:bg-transparent md:hover:text-primary md:p-0">
                Home
            </a>
        </li>
        <li>
            <a href="#" class="py-2 px-3 font-overpass uppercase text-gray-900 rounded hover:bg-gray-100 md:hover:bg-transparent md:hover:text-primary md:p-0">
                Services
            </a>
        </li>
        <li>
            <a href="#" class="py-2 px-3 font-overpass uppercase text-gray-900 rounded hover:bg-gray-100 md:hover:bg-transparent md:hover:text-primary md:p-0">
                Clients
            </a>
        </li>
        <li>
            <a href="#" class="py-2 px-3 font-overpass uppercase text-gray-900 rounded hover:bg-gray-100 md:hover:bg-transparent md:hover:text-primary md:p-0">
                Team
            </a>
        </li>
        <li>
            <a href="#" class="py-2 px-3 font-overpass uppercase text-gray-900 rounded hover:bg-gray-100 md:hover:bg-transparent md:hover:text-primary md:p-0">
                Podcast
            </a>
        </li>
        <li>
            <a href="#" class="py-2 px-3 font-overpass uppercase text-gray-900 rounded hover:bg-gray-100 md:hover:bg-transparent md:hover:text-primary md:p-0">
                Lab
            </a>
        </li>
        <li>
            <a href="#" class="py-2 px-3 font-overpass uppercase text-gray-900 rounded hover:bg-gray-100 md:hover:bg-transparent md:hover:text-primary md:p-0">
                Contact
            </a>
        </li>
      </ul>
    </div>
    </div>
  </nav>
  

  <script>
    window.addEventListener('scroll', () => {
      const header = document.getElementById('navHeader');
      if (window.scrollY > 1) {
        header?.classList.add('bg-white', 'shadow-lg');
        header?.classList.remove('bg-transparent');
      } else {
        header?.classList.remove('bg-white', 'shadow-lg');
        header?.classList.add('bg-transparent');

      }
    });
  </script>